{
  "name": "tankfire",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "format": "prettier --write backend/src backend/scripts backend/test backend/setup-db.js frontend/src frontend/index.html shared eslint.config.mjs package.json backend/package.json frontend/package.json",
    "format:check": "prettier --check backend/src backend/scripts backend/test backend/setup-db.js frontend/src frontend/index.html shared eslint.config.mjs package.json backend/package.json frontend/package.json",
    "lint": "eslint backend/src backend/scripts backend/test backend/setup-db.js frontend/src shared eslint.config.mjs",
    "rebuild:ranking": "npm run rebuild:ranking --workspace backend",
    "test": "npm test --workspace backend",
    "build": "npm run build --workspace frontend",
    "check": "npm run format:check && npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "prettier": "^3.9.6"
  }
}
